---
# defaults file for bastion
users_count: 3
users_numbers: "{{ query('sequence', 'start=0 end={{users_count}} format=%02d') }}"
project_path: "project"
multi_bastion: False
control_docker_via_docker: False

